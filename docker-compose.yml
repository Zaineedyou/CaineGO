services:
  caine:
    build: .
    container_name: caine-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Persist SQLite database di luar container
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/caine.db
